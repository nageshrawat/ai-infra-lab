# Day 4 Learning Summary – AI Infra & LLMOps Journey

## Topics Learned
- Docker fundamentals
- Containerization concepts
- Docker images vs containers
- Docker lifecycle
- Docker networking
- Dockerfile creation
- .dockerignore usage
- Environment variables
- .env configuration
- Docker debugging
- Container logs
- Linux runtime inside containers

---

# What I Built Today

Containerized my FastAPI Infrastructure Incident Management API using Docker.

Successfully:
- Built custom Docker image
- Ran FastAPI application inside Docker container
- Accessed Swagger UI from containerized app
- Injected environment variables into container
- Debugged failed containers using logs

---

# Important Docker Concepts Learned

## Docker Image
A blueprint/template used to create containers.

Example:
```text
incident-api:latest
Docker Container

Running instance of an image.

Containers are isolated runtime environments.

Important Docker Commands Practiced
docker --version
docker info
docker images
docker ps
docker ps -a
docker build -t incident-api .
docker run -d --name infra-api -p 8000:8000 incident-api
docker logs infra-api
docker exec -it infra-api sh
docker stop infra-api
docker rm infra-api
docker rmi incident-api
Dockerfile Understanding

Learned purpose of:

FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt
COPY . .
CMD ["uvicorn", "server:app", "--host", "0.0.0.0", "--port", "8000"]

Understood:

base image
working directory
dependency installation
copying project files
startup command
Image Optimization

Changed:

FROM python:3.11

to:

FROM python:3.11-slim

Reduced image size significantly:

From ~1.65GB
To ~245MB

Learned importance of lightweight production images.

Environment Variables Learned

Created .env file:

APP_NAME=Infra Incident API
APP_ENV=development
APP_PORT=8000

Loaded variables using:

from dotenv import load_dotenv
import os

Understood:

externalized configuration
secure configuration management
production-style runtime configs
Docker Environment Variable Injection

Learned to pass variables into containers:

docker run -e APP_ENV=production

Understood:

same image can run in multiple environments
configuration changes without code modification
Linux Concepts Learned Inside Container

Explored:

/proc
container filesystem
/etc/hosts
environment variables
overlay filesystem

Understood containers are isolated Linux runtime environments.

Important Debugging Lessons

Debugged failed container startup using:

docker logs infra-api

Identified missing dependency:

ModuleNotFoundError: No module named 'dotenv'

Learned:

Docker images are immutable snapshots
images must be rebuilt after dependency/code changes
Important Engineering Understanding

Learned difference between:

local source code
Docker image
running container

Understood:

containers do not auto-update after code changes
rebuild is required for updated deployments
AI Infrastructure Connection

Understood that modern AI systems commonly run inside containers:

FastAPI services
LLM servers
Vector databases
Redis
PostgreSQL
AI inference systems

Realized Docker is foundational for:

AI Infrastructure
LLMOps
Kubernetes
Cloud-native systems
What Excited Me
Running my API inside Docker
Accessing Swagger from containerized application
Exploring Linux shell inside container
Understanding how production deployments work
Successfully debugging container failures
What Confused Me Initially
Difference between images and containers
Why rebuilding images is required
Why containers exited automatically
Docker networking and port mapping

Concepts became clearer after hands-on debugging.